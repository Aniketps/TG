<!DOCTYPE html>
<html>
<head>
    <title>List of Users</title>
    <style>
        *{
            padding: 0;
            margin: 0;
            box-sizing: border-box;
        }
        table{
            border: 1px solid black;
            margin: auto;
            border-collapse: collapse;
            padding: 20px;
        }
        th,td,tr{
            border: 1px solid black;
            padding: 10px;
        }
    </style>
</head>
<body>
    <a href="/addUser">Add new User</a>
    <table>
        <tr>
            <th>Sr no.</th>
            <th>First Name</th>
            <th>Last Name</th>
            <th>Email</th>
            <th>Phone no.</th>
            <th>Delete</th>
            <th>Update</th>
            <th>Profile</th>
        </tr>
        <%
        if(users.length != 0){
            users.forEach((row, index)=>{
                %>
                <tr>
                    <td><%=index+1%></td>
                    <td><%=row.firstName%></td>
                    <td><%=row.lastName%></td>
                    <td><%=row.email%></td>
                    <td><%=row.phone%></td>
                    <td><a href="/deleteuser?id=<%=row.id%>">Delete</a></td>
                    <td><button style="border: none; background-color: transparent;">Update</button></td>
                    <td><a href="/userProfile?id=<%=row.id%>">View</a></td>
                </tr>
                <%
            })
        }else{
            %>
            <tr><td>There is no data of users</td></tr>
            <%
        }
        %>
    </table>
</body>
</html>